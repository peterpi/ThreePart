services:


  db:
    build: db
    volumes:
      - db-data:/var/lib/postgresql


  api:
    build:
      context: api
      target: prod
    depends_on:
      - db


  frontend:
    build: frontend
    depends_on:
      - api

volumes:
  db-data:

