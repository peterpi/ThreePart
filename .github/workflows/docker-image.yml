name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7
    - name: Build the Docker image
      env:
        DOCKER_HUB_PAT: ${{ secrets.DOCKER_HUB_PAT }}
      run: ./.github/workflows/build-images.sh

